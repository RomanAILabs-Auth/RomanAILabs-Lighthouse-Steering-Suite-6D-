# RomanAILabs-Lighthouse-Steering-Suite-6D-
Runtime steering layer for GGUF llama.cpp runs: computes Lighthouse FCES from sampling signals, drives WhiteHole steering scalar, and applies RAIL-FPMF 6D controller to shape logits + optional EOS nudge. Supports run + exact-token bench.
